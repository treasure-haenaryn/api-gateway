global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: api-gateway
    static_configs:
      - targets:
          - host.docker.internal:8080  # 로컬 실행 중인 게이트웨이
    metrics_path: /actuator/prometheus
